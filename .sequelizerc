const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "dist", "config", "migrations.js"),
  "migrations-path": path.resolve(__dirname, "dist", "database", "migrations"),
};
